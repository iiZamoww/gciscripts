local ReplicatedStorage = game:GetService("ReplicatedStorage")

require(ReplicatedStorage.Data)
require(ReplicatedStorage.Currencies)
require(ReplicatedStorage.LethalNum)

return {
    Corruptors = {
        p = {}
    }
}
